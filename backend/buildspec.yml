version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - echo "Installing backend dependencies..."
      - npm install
  build:
    commands:
      - echo "Starting backend server..."
      - nohup node server.js &
artifacts:
  files:
    - '**/*'
  base-directory: .
